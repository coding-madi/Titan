meta {
  name: localhost:8888/sql simple
  type: http
  seq: 3
}

post {
  url: {{baseUrl}}/sql
  body: json
  auth: inherit
}

body:json {
  {
    "sql": "select  * from simple  limit 2000",
    "stream_name": "simple"
  }
}

settings {
  encodeUrl: true
}
