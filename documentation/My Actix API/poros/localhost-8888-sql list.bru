meta {
  name: localhost:8888/sql list
  type: http
  seq: 4
}

post {
  url: {{baseUrl}}/sql
  body: json
  auth: inherit
}

body:json {
  {
    "sql": "select count(*) from log limit 200",
    "stream_name": "log"
  }
}

settings {
  encodeUrl: true
}
