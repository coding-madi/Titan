meta {
  name: localhost:8888/sql
  type: http
  seq: 4
}

post {
  url: {{baseUrl}}/sql
  body: json
  auth: inherit
}

body:json {
  {
    "sql": "select  * from log_list a join log b on a.event_id = b.event_id limit 60000",
    "stream_name": "log_list"
  }
}

settings {
  encodeUrl: true
}
