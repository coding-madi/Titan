meta {
  name: localhost:8888/sql
  type: http
  seq: 4
}

post {
  url: {{baseUrl}}/sql
  body: json
  auth: inherit
}

body:json {
  {
    "sql": "select struct_field_2 from log where struct_field_2.nested_int_2 < 500 limit 100",
    "stream_name": "log"
  }
}

settings {
  encodeUrl: true
}
