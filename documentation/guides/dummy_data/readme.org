cd /home/balaji/Documents/repositories/poros/documentation/guides/dummy_data && echo "Starting 5 parallel instances of script_for_flight_server.py..." && for i in {1..5}; do echo "Starting instance $i..."; python script_for_flight_server.py &done && echo "All 5 instances started. PIDs:" && jobs -p && echo -e "\n=== Monitoring system load ===" && echo "CPU and Memory usage:" && top -b -n1 | head -5 && echo -e "\nActive Python processes:" && ps aux | grep "script_for_flight_server.py" | grep -v grep
